{
  "name": "@videosync/userscript",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Tampermonkey shim. A shipping target in its own right (D5), and the cheapest way to validate the adapter layer against a real provider.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
